# 🗺️ Приложение поиска пользователей на карте

Веб-приложение для поиска пользователей в определенном радиусе от заданных координат или адреса.

## 🚀 Технологии

- **Frontend**: React + TypeScript + Vite + Яндекс.Карты
- **Backend**: Node.js + Express + TypeScript
- **База данных**: PostgreSQL + PostGIS
- **Геокодинг**: OpenStreetMap (через node-geocoder)

## 📋 Предварительные требования

- Node.js (версия 18 или выше)
- PostgreSQL (версия 12 или выше)
- PostGIS (расширение для PostgreSQL)
- npm или yarn

## 🛠️ Установка

### 1. Установка PostgreSQL с PostGIS

Если еще не установлено:

1. Скачайте и установите PostgreSQL с официального сайта: https://www.postgresql.org/download/
2. При установке выберите компонент **PostGIS** (или установите отдельно через Stack Builder)
3. Запомните пароль пользователя `postgres`

### 2. Создание базы данных

Откройте командную строку (PowerShell или CMD) и выполните:

```bash
# Создать базу данных
createdb -U postgres user_map_db
```

Если запросит пароль, введите пароль пользователя postgres.

### 3. Настройка базы данных

Выполните SQL скрипт для создания таблиц:

```bash
psql -U postgres -d user_map_db -f database/init.sql
```

Или вручную в psql:

```bash
psql -U postgres -d user_map_db
```

Затем выполните SQL из файла `database/init.sql`.

### 4. Настройка Backend

```bash
cd backend
npm install
```

Создайте файл `.env` в папке `backend` (скопируйте из `.env.example`):

```env
PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=user_map_db
DB_USER=postgres
DB_PASSWORD=ваш_пароль_postgres
GEOCODER_PROVIDER=openstreetmap
```

**Важно**: Замените `ваш_пароль_postgres` на реальный пароль от PostgreSQL!

### 5. Настройка Frontend

```bash
cd frontend
npm install
```

**Важно**: Для работы карт необходим API ключ Яндекс.Карт.

1. Получите бесплатный API ключ на https://developer.tech.yandex.ru/
2. Создайте файл `.env` в папке `frontend`:
```env
VITE_YANDEX_MAPS_API_KEY=ваш_api_ключ_здесь
```

Без API ключа карты могут не работать или работать с ограничениями.

## 🎯 Запуск

### Запуск Backend

```bash
cd backend
npm run dev
```

Сервер запустится на http://localhost:3001

### Запуск Frontend

В новом терминале:

```bash
cd frontend
npm run dev
```

Приложение откроется на http://localhost:3000

## 📖 Использование

1. **Добавление пользователя**:
   - Заполните форму: имя, телефон, адрес (с номером дома)
   - Нажмите "Создать пользователя"
   - Пользователь появится на карте

2. **Поиск пользователей**:
   - Выберите тип поиска: по адресу или по координатам
   - Укажите радиус поиска в метрах
   - Нажмите "Найти пользователей"
   - На карте отобразится круг поиска и найденные пользователи

3. **Просмотр всех пользователей**:
   - При загрузке приложения автоматически загружаются все пользователи
   - Все маркеры отображаются на карте

## 🔌 API Endpoints

- `POST /api/users` - Создать пользователя
  ```json
  {
    "name": "Иван Иванов",
    "phone": "+7 999 123-45-67",
    "address": "Москва, Красная площадь, 1"
  }
  ```

- `GET /api/users/search?latitude=X&longitude=Y&radius=Z` - Поиск в радиусе по координатам
- `GET /api/users/search?address=...&radius=Z` - Поиск в радиусе по адресу
- `GET /api/users` - Получить всех пользователей
- `GET /health` - Проверка работоспособности сервера

## 🎨 Особенности

- ✅ Красивый современный UI с градиентами и анимациями
- ✅ Интерактивная карта с маркерами пользователей
- ✅ Поиск по радиусу с визуализацией области поиска
- ✅ Геокодинг адресов в координаты
- ✅ Отображение расстояния до найденных пользователей
- ✅ Адаптивный дизайн

## 🐛 Решение проблем

### Ошибка подключения к базе данных

- Проверьте, что PostgreSQL запущен
- Убедитесь, что пароль в `.env` правильный
- Проверьте, что база данных `user_map_db` создана

### Ошибка "PostGIS не найден"

- Убедитесь, что PostGIS установлен
- Выполните: `CREATE EXTENSION postgis;` в базе данных

### Порт занят

- Измените порт в `.env` (backend) или `vite.config.ts` (frontend)

### Ошибка геокодинга адреса

- Проверьте интернет-соединение
- Убедитесь, что адрес указан правильно (город, улица, номер дома)

## 📝 Структура проекта

```
mvp_ver1/
├── backend/
│   ├── src/
│   │   ├── config/       # Конфигурация БД
│   │   ├── controllers/  # Контроллеры
│   │   ├── models/       # Модели данных
│   │   ├── routes/       # Маршруты API
│   │   ├── services/     # Сервисы (геокодинг)
│   │   ├── types/        # TypeScript типы
│   │   └── server.ts     # Точка входа
│   ├── package.json
│   └── tsconfig.json
├── frontend/
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── services/     # API сервисы
│   │   ├── types/        # TypeScript типы
│   │   ├── App.tsx       # Главный компонент
│   │   └── main.tsx      # Точка входа
│   ├── package.json
│   └── vite.config.ts
├── database/
│   └── init.sql          # SQL схема
└── README.md
```

## 🚀 Дальнейшее развитие

- [ ] Аутентификация пользователей
- [ ] Фильтры поиска (по имени, телефону)
- [ ] Экспорт данных
- [ ] Мобильная версия
- [ ] Кластеризация маркеров на карте
- [ ] История поисков

## 📄 Лицензия

MIT

---

**Приятного использования! 🎉**

